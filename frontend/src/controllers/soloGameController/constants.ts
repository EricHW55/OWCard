export const SOLO_UI = {
  errorNoticeMs: 1200,
  phaseNoticeMs: 1600,
  roundNoticeMs: 2200,
  maxMulliganSelections: 2,
} as const;

export const SOLO_HP_ANIMATION_MS = 500;
export const SOLO_DESTROY_ANIMATION_MS = 820;
export const SOLO_DAMAGE_FLOAT_MS = 800;
